#sidebar
	#title
		img(src='/images/logo.svg')
	#user
		if typeof(user) !== 'undefined'
			.username #{user.username}
			a.logout(href='/logout') logout
		else 
			a.login(href='/login') login
	#navigation
		.nav-section
			.title Locations
			.nav-list
				each location, i in locations
					if i == 0
						.list-item.location.active(location=location._id) #{location.location.name}
					else 
						.list-item.location(location=location._id) #{location.location.name}
		.nav-section
			a.title(href='/dashboard') Plots
			.nav-list
				each plotItem in location.plots
					if typeof(plot) !== 'undefined' && current.plot == plotItem._id
						.list-item.active.view-data(plot=plotItem._id location=location._id) Plot ##{plotItem.info.plotnumber}	
					else
						.list-item.view-data(plot=plotItem._id location=location._id) Plot ##{plotItem.info.plotnumber}
